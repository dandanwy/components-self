import dragTree from './drag_tree'

// const dragTreeComponent = {
//   install: function(Vue) {
//     if (typeof window !== 'undefined' && window.Vue) {
//       Vue = window.Vue
//     }
//     Vue.component('dragTree', dragTree)
//   }
// }

export default dragTree;